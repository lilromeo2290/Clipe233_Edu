# Worklog Command

Commits and pushes all changes to GitHub repository.

Usage: `/worklog`

## Command
```bash
git add -A && git commit -m "Update worklog" && git push
```

Note: Uses the repository URL configured in git remote.